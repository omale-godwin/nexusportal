
services:
  # ---------------------------------------------------------------------------
  # FLOWISEAI - LLM Workflow / Agent Builder
  # ---------------------------------------------------------------------------
  flowise:
    image: flowiseai/flowise:latest
    container_name: flowise
    restart: unless-stopped
    ports:
      - "3012:3000"
    environment:
      PORT: 3000
      FLOWISE_SIGNUP_DISABLED: "true"
      FLOWISE_SECRETKEY_OVERWRITE: ${FLOWISE_SECRET_KEY}
      DATABASE_TYPE: mongodb
      DATABASE_MONGO_URI: ${FLOWISE_MONGODB_URI}
      APIKEY_PATH: /data/api-keys
      SECRETKEY_PATH: /data/secret-keys
      LOG_PATH: /data/logs
      BLOB_STORAGE_PATH: /data/uploads
      TOOL_FUNCTION_BUILTIN_DEP: crypto,fs
      TOOL_FUNCTION_EXTERNAL_DEP: moment,lodash
      NODE_ENV: production
      FLOWISE_CORS_ORIGINS: "*"
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
      DEEPSEEK_BASE_URL: https://api.deepseek.com/v1
      XAI_API_KEY: ${XAI_API_KEY:-}
      XAI_BASE_URL: https://api.x.ai/v1
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      GOOGLE_API_KEY: ${GOOGLE_API_KEY:-}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      OLLAMA_BASE_URL: http://ollama:11434
    volumes:
      - flowise_data:/data
      - flowise_uploads:/data/uploads
    networks:
      - flowise_net
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000/api/v1/ping"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s


  # ---------------------------------------------------------------------------
  # DIRECTUS CMS - Headless CMS
  # ---------------------------------------------------------------------------
  directus:
    image: directus/directus:latest
    container_name: directus
    restart: unless-stopped
    environment:
      SECRET: ${DIRECTUS_SECRET}
      DB_CLIENT: mysql
      DB_HOST: ${DIRECTUS_DB_HOST}
      DB_PORT: ${DIRECTUS_DB_PORT:-25060}
      DB_DATABASE: ${DIRECTUS_DB_NAME}
      DB_USER: ${DIRECTUS_DB_USER}
      DB_PASSWORD: ${DIRECTUS_DB_PASSWORD}
      DB_SSL__REJECT_UNAUTHORIZED: "false"
      ADMIN_EMAIL: ${DIRECTUS_ADMIN_EMAIL}
      ADMIN_PASSWORD: ${DIRECTUS_ADMIN_PASSWORD}
      PUBLIC_URL: "http://129.212.146.116:8055"
      CORS_ENABLED: "false"
      CORS_ORIGIN: "*"
      REFRESH_TOKEN_COOKIE_SECURE: "false"
      REFRESH_TOKEN_COOKIE_SAME_SITE: "lax"
      STORAGE_LOCATIONS: local
      STORAGE_LOCAL_ROOT: /directus/uploads
      CACHE_ENABLED: "true"
      CACHE_STORE: memory
      LOG_LEVEL: debug
      EMAIL_FROM: ${EMAIL_FROM}
      EMAIL_TRANSPORT: ${EMAIL_TRANSPORT:-smtp}
      EMAIL_SMTP_HOST: ${SMTP_HOST}
      EMAIL_SMTP_PORT: ${SMTP_PORT:-587}
      EMAIL_SMTP_USER: ${SMTP_USER}
      EMAIL_SMTP_PASSWORD: ${SMTP_PASSWORD}
      RATE_LIMITER_ENABLED: "true"
      RATE_LIMITER_POINTS: 100
      RATE_LIMITER_DURATION: 1
      IP_TRUST_PROXY: "true"
      IP_CUSTOM_HEADER: "X-Forwarded-For"
    volumes:
      - directus_uploads:/directus/uploads
      - directus_extensions:/directus/extensions
      - directus_templates:/directus/templates
      - directus_logs:/directus/logs
    ports:
      - "8055:8055"
    networks:
     
      - directus_net
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8055/server/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
  

  # ---------------------------------------------------------------------------
  # OPENCLAW - AI Agent Gateway
  # Official image: ghcr.io/openclaw/openclaw:latest
  # Web dashboard on port 18789, internal gateway API on 18793.
  # The CLI sidecar (openclaw-cli) shares the gateway network namespace so
  # CLI commands reach the gateway reliably over 127.0.0.1.
  # ---------------------------------------------------------------------------
  # One-shot init container — runs before the gateway, fixes volume ownership.
  # Docker named volumes are created as root:root. OpenClaw runs as node (UID
  # 1000 / GID 1000) and cannot write to them without this fix.
  # Automatically skipped on subsequent starts because the condition is met.
  openclaw-init:
    image: busybox:latest
    container_name: openclaw-init
    restart: "no"
    entrypoint: >
      sh -c "
        chown -R 1000:1000 /home/node/.openclaw &&
        chown -R 1000:1000 /home/node/openclaw/workspace &&
        echo 'OpenClaw volume permissions fixed.'
      "
    volumes:
      - openclaw_config:/home/node/.openclaw
      - openclaw_workspace:/home/node/openclaw/workspace
      - openclaw_data:/home/node/.openclaw/data

  openclaw-gateway:
    image: ghcr.io/openclaw/openclaw:latest
    container_name: openclaw-gateway
    restart: unless-stopped
    user: "1000:1000"   # run as node — matches the image's node user
    environment:
      NODE_ENV: production
      # LLM provider keys — add whichever you use
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
      XAI_API_KEY: ${XAI_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      GOOGLE_AI_API_KEY: ${GOOGLE_API_KEY:-}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      # Messaging channel tokens
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      DISCORD_BOT_TOKEN: ${DISCORD_BOT_TOKEN:-}
      # Gateway token locks the web dashboard
      GATEWAY_TOKEN: ${OPENCLAW_GATEWAY_TOKEN}
      # Trust Nginx proxy headers
      OPENCLAW_TRUSTED_PROXIES: "nginx"
    volumes:
      - openclaw_config:/home/node/.openclaw
      - openclaw_workspace:/home/node/openclaw/workspace
      - openclaw_data:/home/node/.openclaw/data
    expose:
      - "18789"
      - "18793"
    networks:
      
      - openclaw_net
    cap_drop:
      - NET_RAW
      - NET_ADMIN
    security_opt:
      - no-new-privileges:true
    depends_on:
      openclaw-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:18789"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  # OpenClaw CLI sidecar
  # Start with: docker compose --profile cli up -d openclaw-cli
  # Run CLI commands: docker compose exec openclaw-cli node dist/index.js <cmd>
  # Examples:
  #   docker compose exec openclaw-cli node dist/index.js dashboard --no-open
  #   docker compose exec openclaw-cli node dist/index.js channels add --channel telegram --token <token>
  openclaw-cli:
    image: ghcr.io/openclaw/openclaw:latest
    container_name: openclaw-cli
    network_mode: "service:openclaw-gateway"
    user: "1000:1000"
    restart: "no"
    entrypoint: ["tail", "-f", "/dev/null"]
    volumes:
      - openclaw_config:/home/node/.openclaw
      - openclaw_workspace:/home/node/openclaw/workspace
      - openclaw_data:/home/node/.openclaw/data
    cap_drop:
      - NET_RAW
      - NET_ADMIN
    security_opt:
      - no-new-privileges:true
    depends_on:
      - openclaw-gateway
    profiles:
      - cli


  
networks:
  flowise_net:
    driver: bridge
    name: flowise_net
  directus_net:
    driver: bridge
    name: directus_net
  openclaw_net:
    driver: bridge
    name: openclaw_net


# ---------------------------------------------------------------------------
# VOLUMES
# ---------------------------------------------------------------------------
volumes:
  flowise_data:
    driver: local
  flowise_uploads:
    driver: local
  directus_uploads:
    driver: local
  directus_extensions:
    driver: local
  directus_templates:
    driver: local
  directus_logs:
    driver: local
  openclaw_config:
    driver: local
  openclaw_workspace:
    driver: local
  openclaw_data:
    driver: local

